# Three-Phase 三元相图计算 — 学习手册

> 写给后续需要独立使用这些脚本的你。
> 不需要懂所有细节，能跑、能改、能看懂结果就行。

---

## 一、快速上手（5 分钟）

### 1.1 打开 MATLAB，切到目录

```matlab
cd d:\Deskkk\First CC\Three-phase
```

### 1.2 运行主脚本

```matlab
FINAL_DELIVERY
```

等大概 2-3 分钟，所有计算和图表就会自动生成到 `output/` 文件夹。

### 1.3 你会得到什么

```
output/
├── binodal_Tol.csv          ← Tol体系的双节点线数据（可导入Origin画图）
├── binodal_OXy.csv          ← O-Xy体系的双节点线数据
├── spinodal_Tol.csv         ← Tol体系的旋节线
├── spinodal_OXy.csv         ← O-Xy体系的旋节线
├── critical_Tol.csv         ← Tol体系临界点
├── critical_OXy.csv         ← O-Xy体系临界点
├── ternary_combined.png     ← 两个体系对比的三元相图
├── ternary_Tol.png          ← Tol体系单独三元相图
├── ternary_OXy.png          ← O-Xy体系单独三元相图
├── ratio_Tol.png            ← Tol体系 给受体比-溶剂图
├── ratio_OXy.png            ← O-Xy体系 给受体比-溶剂图
└── *_HR.png                 ← 以上所有图的高清300dpi版本
```

---

## 二、理解输出数据

### 2.1 binodal CSV 的结构

打开 `output/binodal_Tol.csv`，你会看到：

| residual | phi1_L8Bo | phi3_PM6 | phi2_Tol |
|----------|-----------|----------|----------|
| 1.6e-30 | 0.270 | 0.384 | 0.345 | ← 第1行 = 第1条tie line的浓相
| 1.6e-30 | 0.539 | 1e-5 | 0.461 | ← 第2行 = 第1条tie line的稀相
| 3.2e-30 | 0.272 | 0.380 | 0.348 | ← 第3行 = 第2条tie line的浓相
| 3.2e-30 | 0.537 | 1.1e-5 | 0.463 | ← 第4行 = 第2条tie line的稀相
| ...     | ...   | ...   | ...   |

**关键规律：每两行组成一对**（奇数行 = 浓相，偶数行 = 稀相）。

- **phi1** = L8-Bo 的体积分数
- **phi3** = PM6 的体积分数
- **phi2** = 溶剂的体积分数
- **residual** = 化学势等式的残差（越小越好，< 1e-8 就是合格的）

你可以在 Origin 中直接导入，选择 Ternary 模板，用 phi1/phi3/phi2 三列画三元相图。

### 2.2 如何判断数据质量

看这几条：

1. **浓相 phi3 要单调递减**：从最大值（远离临界点）逐渐降到接近临界点
2. **稀相 phi3 要单调递增**：从极小值（1e-5）逐渐升到接近临界点
3. **residual 要小**：合格的标准是 < 1e-8，我们实际做到的是 1e-30 量级
4. **两个相要有明显区别**：浓相的 PM6 必须比稀相高

### 2.3 Ratio-Solvent 图的读法

这是我们新设计的可视化，比传统三元相图更直观：

- **横轴** = L8-Bo / (L8-Bo + PM6)，即非溶剂部分中 L8-Bo 的占比
  - 接近 0 → 几乎纯 PM6
  - 接近 1 → 几乎纯 L8-Bo
- **纵轴** = 溶剂的体积分数 φ₂
  - 接近 0 → 几乎无溶剂
  - 接近 1 → 几乎纯溶剂

蓝色填充区域 = 两相共存区。如果你选择的体系总组成落在这个区域内，就会分相。

---

## 三、关键脚本说明

### 3.1 FINAL_DELIVERY.m（主脚本，你只需会用这个）

```matlab
FINAL_DELIVERY    % 运行
```

做了什么：
1. 定义两套优化后的参数（Tol 和 O-Xy）
2. 对每个体系：
   - 先在 1200×1200 网格上算旋节线（det(G'')=0）
   - 在旋节线上通过三阶条件找临界点
   - 用 fsolve 解化学势等式，得到 120 条 tie line
   - 过滤掉不合格的点，保证单调性
3. 生成 4 类图（综合三元 / 单独三元 / Ratio-Solvent / 高清版）

**如果你想换参数**，打开文件找到这一段：

```matlab
systems = {
    struct('tag','Tol','name','Toluene','v2',106.3,'X13',0.95,'g23',0.385,'v3',100000,'p',[0,0,0,-0.20,0.60]);
    struct('tag','OXy','name','o-Xylene','v2',120.6,'X13',0.80,'g23',0.412,'v3',120000,'p',[0,0,0,-0.20,0.75]);
};
```

改 `X13`、`g23`、`v3` 这几个数字就行。

### 3.2 param_sweep_v2.m（参数扫描，选出最佳参数）

这是我们做 192 组三维网格扫描的脚本。v3 × X13 × g23。

```matlab
param_sweep_v2    % 运行（约 3-5 分钟）
```

输出：
- `output/param_sweep_v2.mat` — 所有扫描结果
- 热力图（形状评分 / 成功率分布）

**适用场景**：当你拿到一个新的给受体/溶剂体系，不确定该用什么参数时，用它来系统性地探索。

### 3.3 find_best_OXy.m（针对性最佳参数搜索）

针对 O-Xy 体系做的 60 组快速扫描，找到等效于 Tol best_case1 的参数。

```matlab
find_best_OXy    % 运行（约 1 分钟）
```

逻辑：固定 v3，扫描 X13 和 g23，按形状评分排序。

### 3.4 gen_figures.m（从扫描结果重新出图）

如果你运行了参数扫描，但想调整图表的样式，可以只跑这个：

```matlab
gen_figures    % 从 param_sweep_v2.mat 加载结果重新出图
```

---

## 四、如何调参数（核心经验）

三个参数的作用一句话总结：

| 调什么 | 往哪调 | 效果 |
|--------|--------|------|
| **v3** (PM6 摩尔体积) | ↓ 减小 | 相图向中心展开，两臂远离 L8-Bo 角，形状更"规整" |
| **X13** (L8-Bo/PM6 排斥) | ↑ 增大 | 两相区扩大，tie line 变长，临界点 PM6 浓度提高 |
| **g23** (溶剂/PM6 排斥) | ↑ 增大 | 补偿 v3 降低时损失的分相驱动力，拓宽相区 |

调参口诀：

```
降 v3  = 相图拉宽（但如果分相驱动力不够，两相区会消失）
升 X13 = 相区扩大（但如果太大，形状会失真）
升 g23 = 补偿分相驱动力（当降低 v3 导致分相困难时用）
```

**实用调参流程**：

1. 先确定你要的 v3（PM6 分子量/聚合度）
2. 调 X13，使得成功率 > 80%
3. 如果 X13 调太高了还不成功 → 提高 g23
4. 检查曲线形状：浓相 phi3 应该从高到低单调变化

---

## 五、物理背景速查

### 5.1 Flory-Huggins 理论（你需要知道的）

这是高分子溶液热力学的基本理论。对于三元体系，混合自由能：

```
ΔG_mix/RT = (φ₁/v₁)ln(φ₁) + (φ₂/v₂)ln(φ₂) + (φ₃/v₃)ln(φ₃)
           + g₁₂·φ₁φ₂ + χ₁₃·φ₁φ₃ + g₂₃·φ₂φ₃
```

- 前三项是**混合熵**（总是负的，有利于混合）
- 后三项是**混合焓**（正的 = 排斥，不利于混合）

**相分离的条件**：焓的排斥作用 > 熵的混合作用。

- 如果各组分体积差不多 → 熵的作用大 → 不容易分相 → 需要更大的排斥参数
- 如果 PM6 体积巨大（v3 大）→ 熵的作用小 → 容易分相 → 很小的排斥就够

这就是为什么 v3 对相图形状影响这么大。

### 5.2 三个相互作用参数的含义

- **X13** (L8-Bo / PM6)：受体和给体之间的排斥
  - X13 > 0.5 → 倾向于分相
  - X13 < 0 → 倾向于混合
  
- **g23** (溶剂 / PM6)：溶剂和聚合物的排斥
  - g23 > 0.5 → 溶剂"不好"，聚合物容易析出
  - g23 < 0.3 → 溶剂"好"，聚合物溶解良好

- **g12** (L8-Bo / 溶剂)：受体和溶剂的排斥
  - 我们用的是线性依赖：g₁₂ = -0.2·u₂ + (0.60 或 0.75)
  - u₂ 是溶剂占比，所以 g12 随溶剂浓度变化

### 5.3 相图的几个关键特征

- **Binodal（双节点线）** = 两相共存边界。线外的组成是单相，线内是两相共存
- **Spinodal（旋节线）** = 亚稳边界。binodal 和 spinodal 之间是亚稳区
- **Critical Point（临界点）** = binodal 和 spinodal 的交点，此处两相组成相同
- **Tie Line（结线 / 连接线）** = 连接互相平衡的两相组成

---

## 六、常见问题排查

### Q1: 运行后成功率很低（< 50%）

可能原因：
- X13 太小 → 增大 X13
- g23 太小 → 增大 g23
- v3 太小 → 先增大 v3 试试，或者同时增大 X13 和 g23

### Q2: 曲线有跳点（浓相 phi3 不单调）

可能原因：
- fsolve 在某些点收敛到了错误解
- 解决方法：
  1. 增大 n_loop（更多采样点）
  2. 添加更多起始猜测（guesses 数组）
  3. 检查 residual 异常大的点

脚本里的单调性过滤器会自动移除跳点，但不如从源头解决。

### Q3: 临界点找不到（crit_val 不变号）

可能原因：
- 网格分辨率不够 → 增大 n_g（当前 1200）
- 参数导致临界点在网格边缘 → 微调 X13

### Q4: 相图太窄（全部挤在一条边上）

这就是原始参数的问题（v3=1.74M 太大了）。
解决方法：降低 v3（比如从 1.74M 降到 100K），同时提高 X13 补偿。

### Q5: Origin 导入 CSV 后相图不对称

检查：
- 是否选了正确的三列（phi1, phi3, phi2）
- Origin 的 Ternary 模板的顶点顺序：左下=phi3(PM6), 右下=phi1(L8-Bo), 上=phi2(Solvent)

---

## 七、目录结构速查

```
Three-phase/
│
├── FINAL_DELIVERY.m        ← 【你主要用这个】主脚本
├── OUTPUT_README.md        ← 输出文件说明
├── LEARNING_GUIDE.md       ← 本文件
├── 调试说明.md             ← 早期调试历史（参考用）
│
├── param_sweep.m           ← V1: v3×X13 扫描
├── param_sweep_v2.m        ← V2: v3×X13×g23 三维扫描
├── find_best_OXy.m         ← O-Xy 最佳参数搜索
├── gen_figures.m           ← 从扫描结果出图
│
├── output/                 ← 所有生成的文件
│   ├── binodal_*.csv       ← 双节点线数据
│   ├── spinodal_*.csv      ← 旋节线数据
│   ├── critical_*.csv      ← 临界点数据
│   ├── ternary_*.png       ← 三元相图
│   ├── ratio_*.png         ← Ratio-Solvent 相图
│   └── *_HR.png            ← 高清版
│
├── src/                    ← 原始源码（调试历史）
│   ├── binodal-PM6-L8Bo-Tol/
│   ├── binodal-PM6-L8Bo-OXy/
│   ├── spinodal-gemini/
│   ├── Critical_point/
│   └── debug-tools/
│
└── run_all.m               ← 旧版入口（已弃用）
```

---

## 八、如果你想加一个新的溶剂体系

以添加 "Chlorobenzene (CB)" 为例：

### Step 1: 收集参数
- v2 = CB 摩尔体积
- g23 = CB/PM6 相互作用参数（可用 UNIFAC 估算）
- p1-p5 = L8-Bo/CB 的 g12 多项式系数（可用 HSP 估算）

### Step 2: 添加到 FINAL_DELIVERY.m
在 `systems` 数组中加一行：
```matlab
struct('tag','CB','name','Chlorobenzene','v2',102.2,'X13',0.90,'g23',0.40,'v3',100000,'p',[0,0,0,-0.20,0.55]);
```

### Step 3: 如果不确定参数
用 param_sweep_v2.m 扫描：
```matlab
% 在 param_sweep_v2.m 中修改 v2 和 p 系数
% 设置 v3_values, X13_values, g23_values 的扫描范围
% 运行后查看 shape_score 热力图
```

---

## 九、快速命令速查

```matlab
% 日常使用
FINAL_DELIVERY              % 一键生成所有结果

% 参数探索
param_sweep_v2              % 三维扫描找最佳参数
gen_figures                 % 从扫描结果重新出图

% 数据查看
T = readtable('output/binodal_Tol.csv');  % 读取数据
head(T)                                  % 看前几行
size(T,1)/2                              % 查看有多少对 tie line
```
